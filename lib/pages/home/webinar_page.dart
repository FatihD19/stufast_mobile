import 'package:flutter/material.dart';

class WebinarPage extends StatelessWidget {
  const WebinarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Webinar Page'),
    );
  }
}
