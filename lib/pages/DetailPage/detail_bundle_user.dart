import 'package:flutter/material.dart';

class DetailBundle extends StatelessWidget {
  const DetailBundle({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
          child: Center(
        child: Text('Detail bundle'),
      )),
    );
  }
}
