import 'package:flutter/material.dart';

class TalentHubPage extends StatelessWidget {
  const TalentHubPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Talent HUB'),
      ),
    );
  }
}
